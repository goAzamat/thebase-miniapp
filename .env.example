# ------------------------------------------------------------------
# Copy to .env.local for development. In production set these in the
# Vercel Dashboard → Project → Settings → Environment Variables.
# ------------------------------------------------------------------

# --- Odoo XML-RPC connection ---
ODOO_URL=https://odoo.thebasebev.com
ODOO_DB=qualityofdark-thebasebevodoo-production-30781491
# Service account used for all dashboard reads (needs read access to
# project.task, crm.lead, mrp.bom, mrp.bom.line, product.template,
# product.product, product.category).
ODOO_USERNAME=portal-service@thebasebev.com
ODOO_API_KEY=replace-with-odoo-api-key
# Fallback vendor (res.partner id) for draft Purchase Orders when a product has
# no supplier mapped in product.supplierinfo. Leave empty to skip PO creation.
ODOO_DEFAULT_VENDOR_ID=

# --- Auth.js (NextAuth v5) ---
# Generate with:  npx auth secret
AUTH_SECRET=replace-with-generated-secret

# --- WhatsApp Business API webhook (Meta Cloud API) ---
# Verify token you set in the Meta App webhook config (GET handshake).
WHATSAPP_WEBHOOK_VERIFY_TOKEN=choose-a-long-random-token
# Optional: Meta App Secret → enables X-Hub-Signature-256 POST verification.
WHATSAPP_APP_SECRET=replace-with-meta-app-secret
